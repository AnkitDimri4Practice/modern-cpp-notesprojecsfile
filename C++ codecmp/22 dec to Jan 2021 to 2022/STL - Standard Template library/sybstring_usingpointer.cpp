#include<iostream>
#include <string>
using namespace std;
int main() {
    string str;
    po(&str);
}
void po(string *str){
    string t = str->substring(a, b);; //You can use the pointer de-reference operator ->
}